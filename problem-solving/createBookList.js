// createBookList
// takes three parameters =>  array of bookTitles, array of book authorNames, an array of corresponding copiesAvailable
// addCopies
// takes two parameters => bookTitle, number of copies available

// sellBook that takes two parameters : `bookTitle` and the number of copies you are selling of that book. The function should deduct copies of that particular book.

// getDetails that takes no parameters and returns all the titles of the book in the bookTitles array ,the author names of the book in the `authorNames’ array and the number of copies available of the corresponding books, which should be updated after adding or selling a book.
